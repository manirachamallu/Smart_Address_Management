# Temporary Address Lifecycle Management — Foodie Prototype

Standalone React + TypeScript + Tailwind v4 prototype (Vite).

```bash
npm install
npm run dev     # http://localhost:8080
npm run build
```
